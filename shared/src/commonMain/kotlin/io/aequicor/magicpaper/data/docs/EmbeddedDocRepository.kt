package io.aequicor.magicpaper.data.docs

import io.aequicor.magicpaper.domain.DocArticle
import io.aequicor.magicpaper.domain.DocMatch
import io.aequicor.magicpaper.domain.DocRepository
import kotlin.math.ln
import kotlin.math.sqrt

/**
 * Встроенная документация приложения.
 * Статьи зашиты в общий код и доступны на всех платформах без сети.
 * Поиск — простой векторный (косинусная близость по токенам), без внешних библиотек.
 */
class EmbeddedDocRepository : DocRepository {

    private val articles: List<DocArticle> = buildArticles()

    override suspend fun articles(): List<DocArticle> = articles

    override suspend fun search(query: String, limit: Int): List<DocMatch> {
        val qTokens = tokenize(query)
        if (qTokens.isEmpty()) return emptyList()
        val qVector = vector(qTokens)
        return articles
            .map { article ->
                val aVector = vector(tokenize(article.title + " " + article.title + " " + article.body))
                article to cosine(qVector, aVector)
            }
            .filter { it.second > 0.0 }
            .sortedByDescending { it.second }
            .take(limit)
            .map { DocMatch(it.first, it.second) }
    }

    private fun tokenize(text: String): List<String> =
        text.lowercase()
            .split(' ', '\n', '\t', '.', ',', ';', ':', '!', '?', '(', ')', '-', '"', '\'', '/')
            .map { it.trim() }
            .filter { it.length > 1 }

    private fun vector(tokens: List<String>): Map<String, Double> {
        val counts = tokens.groupingBy { it }.eachCount()
        val total = counts.values.sum().toDouble().coerceAtLeast(1.0)
        return counts.mapValues { (_, c) -> c / total }
    }

    private fun cosine(a: Map<String, Double>, b: Map<String, Double>): Double {
        if (a.isEmpty() || b.isEmpty()) return 0.0
        val dot = a.entries.sumOf { (k, v) -> v * (b[k] ?: 0.0) }
        val na = sqrt(a.values.sumOf { it * it })
        val nb = sqrt(b.values.sumOf { it * it })
        if (na == 0.0 || nb == 0.0) return 0.0
        return dot / (na * nb)
    }

    private fun buildArticles(): List<DocArticle> = listOf(
        DocArticle(
            id = "start",
            title = "Быстрый старт",
            body = """
                MagicPaper — это волшебный блокнот-ассистент. Напишите вопрос в поле заклинаний
                внизу, и агент ответит, при необходимости обратившись к поиску.
                Интерфейс нарочито минималистичен: один экран, один диалог, одна цель — помочь вам.
            """.trimIndent()
        ),
        DocArticle(
            id = "search",
            title = "Поисковый движок",
            body = """
                Agent умеет искать в интернете. В настройках выберите движок:
                Wikipedia (работает без ключей), Querit.ai, Google Programmable Search или AUTO.
                AUTO — сначала пробует настроенный ключевой движок, иначе Wikipedia.
                Ключи задаются на экране настроек и хранятся локально.
            """.trimIndent()
        ),
        DocArticle(
            id = "llm",
            title = "Магические источники: провайдеры и модели",
            body = """
                MagicPaper подключается к нескольким ИИ-провайдерам одновременно — каждый
                оформляется отдельным «источником» (профилем подключения). Поддерживаются
                любой OpenAI-совместимый сервер (Ollama, LM Studio, vLLM, OpenRouter, OpenAI),
                Anthropic (Claude) и Google Gemini. Добавьте источник в настройках
                («Магические источники» → «Подключить провайдера»): выберите провайдера,
                укажите ключ (для локальных серверов не нужен) и модель.

                В desktop можно выбрать «OpenAI (подписка ChatGPT)»: Base URL и API-ключ
                не нужны, вход открывается в браузере, а список моделей и лимиты подписки
                читаются через официальный Codex app-server. Авторизация MagicPaper хранится
                отдельно от обычного Codex. На Android и Web этот источник отключён.
                На desktop он доступен и в чате, и в проектных coding-сессиях.

                У каждого источника есть усилие — единая шкала 0–100 с быстрыми пресетами:
                модели с нативным управлением усилием получают его в родном формате,
                остальные — температурным режимом. В «Тонких настройках» задаются
                температура, максимум токенов, таймаут и глубина истории.

                Переключение источника — отдельной кнопкой прямо в чате: тап по чипу модели
                над полем заклинаний открывает список источников. Выбор действует для текущего
                свитка; кнопка «основной» делает источник активным для всех. Кнопка переключения
                работает в любой момент — посреди разговора история не теряется.

                В редакторе источника есть «Запросить список моделей» — приложение скачивает
                доступные модели прямо у провайдера и помечает, какие из них поддерживают
                нативное усилие; тап по модели применяет её рекомендуемые параметры,
                звезда ★ добавляет модель в избранное. Избранные модели показываются при выборе модели в чате и кодинг-сессиях.
                «Проверить подключение» отправляет тестовый запрос и показывает результат.
                Ключи хранятся локально и входят в файл экспорта профиля.
            """.trimIndent()
        ),
        DocArticle(
            id = "plugins",
            title = "Плагины",
            body = """
                Приложение расширяется плагинами. Каждый плагин добавляет отдельную панель
                к минималистичному интерфейсу. Включайте и выключайте их на экране плагинов.
                В комплекте: заметки (заклинания памяти), таймер фокуса и калькулятор.
            """.trimIndent()
        ),
        DocArticle(
            id = "profile",
            title = "Перенос профиля и удаление",
            body = """
                На экране настроек можно экспортировать профиль в файл: он содержит настройки,
                состояние плагинов и всю историю чатов. Импортируйте файл на другом устройстве,
                чтобы перенести всё. Приложение работает в изолированной среде пользователя и не
                требует прав администратора; при удалении программы удаляются и все данные.
            """.trimIndent()
        ),
        DocArticle(
            id = "privacy",
            title = "Безопасность данных",
            body = """
                Данные хранятся локально в папке пользователя (desktop) или в localStorage браузера.
                Приложение не требует установки и прав администратора. Ключи и история не покидают
                устройство, кроме запросов к выбранным вами поисковым и ИИ-провайдерам.
            """.trimIndent()
        ),
        DocArticle(
            id = "hotkeys",
            title = "Горячие клавиши",
            body = """
                Enter — отправить заклинание. Shift+Enter — новая строка.
                На десктопе окно можно перетаскивать за верхнюю панель.
            """.trimIndent()
        ),
        DocArticle(
            id = "skills",
            title = "Лавка навыков",
            body = """
                Навыки — это проверенные инструкции, которые агент подхватывает автоматически.
                Плагин «Лавка навыков» содержит каталог проверенных временем скиллов: резюме,
                вычитка, планы, сравнения и другие. Установка — в один клик; навык можно
                выключить переключателем или удалить. Каталог живёт в коде и работает офлайн,
                а поиск по нему находит навык по описанию задачи. Навыки входят в экспорт
                профиля и переносятся вместе с историей.
            """.trimIndent()
        ),
        DocArticle(
            id = "self-education",
            title = "Самообучение агента",
            body = """
                Плагин «Самообучение» позволяет агенту самому создавать навыки из диалога —
                как тренировка памяти. После удачного ответа нажмите «Предложить навык»:
                агент сформулирует имя, назначение и инструкцию черновика. Проверьте и
                отредактируйте текст, затем сохраните — навык появится в библиотеке и будет
                автоматически применяться в похожих задачах. Если модель не подключена,
                черновик создаётся из последнего запроса и помечается как «без модели».
                Никакой навык не сохраняется без вашего подтверждения.
            """.trimIndent()
        ),
        DocArticle(
            id = "coding",
            title = "Проекты и код (кодинг-агент)",
            body = """
                Раздел «Проекты и код» подключает кодинг-агент (движок пи-агент) к вашим
                проектам. Проект — это рабочая папка: агент читает её файлы, выполняет запросы
                прямо в ней и продолжает контекст между запусками (сессии сохраняются).
                Добавление проекта: нативный выбор папки. Движок ставится автоматически при
                первом обращении — изолированно, в папке данных приложения: если на компьютере
                нет подходящего Node, скачивается его официальный дистрибутив; ничего не
                ставится в систему. Кнопка «Удалить зависимости» очищает всё. Модель для агента —
                та же, что настроена в «Магическом источнике» (любой OpenAI-совместимый сервер).
                Доступно в десктопной версии.
            """.trimIndent()
        ),
        DocArticle(
            id = "planning",
            title = "Планирование кодинг-задач",
            body = """
                Планирование — дерево решений проекта. Диалог рядом с графом уточняет цель,
                критерии успеха и ограничения. Варианты доступны на любом уровне; оценки
                качества, скорости, экономичности и надёжности относительные. Приоритеты
                настраиваются, ручные решения сохраняются при пересчёте.
                Выберите узел графа, чтобы изменить будущий этап, зависимости, модель или effort.
                Граф масштабируется и перемещается; ветви можно свернуть. Линии обозначают
                состав и варианты, пунктир — зависимости. Отчёты и ошибки доступны у этапов.
                «Утвердить и запустить» начинает реализацию в Desktop. Независимые этапы
                Git-проекта работают в отдельных копиях; параллельность от 1 до 8, по умолчанию 2.
                Без Git выполняется один этап за раз. Результаты проходят проверку перед
                объединением; конфликт сначала пытается разрешить агент. Перед переносом
                в исходную папку проверяется объединённый результат и пользовательские изменения.
                После закрытия или сбоя приложения запущенная работа автоматически продолжается.
                «Пауза» и «Остановить» сохраняют намерение пользователя и отменяют автопродолжение.
                При неизвестных последствиях прерванной команды сначала проверьте результат,
                затем нажмите «Повторить после исправления». Счётчики повторов не сбрасываются.
                На Web и Android доступны подготовка и редактирование; выполнение — в Desktop.
                Досье описывают конкретную модель источника. На проект хранится один актуальный план.
            """.trimIndent()
        ),
        DocArticle(
            id = "attachments",
            title = "Вложения: изображения и файлы",
            body = """
                К сообщению можно прикрепить файлы — кнопка со скрепкой рядом с полем ввода
                (в чате и в кодинг-сессиях). Изображения отправляются модели напрямую,
                если модель поддерживает зрение; текстовые файлы подмешиваются в сообщение
                содержимым. На одно сообщение — до 8 вложений; превью можно раскрыть,
                кликнув по нему. В кодинг-сессиях сами файлы раскладываются в изолированную
                папку рантайма, а агенту передаются пути к ним — он читает их своими
                инструментами. Лимит размера файла зависит от платформы: на десктопе
                свободнее, в браузере жёстче (история хранится в локальном хранилище).
            """.trimIndent()
        ),
    )
}
