# Пространство состояний машин

Общий документ для всех владельцев состояния. Он отвечает на три вопроса: что значит «машина
объявила пространство состояний», как это сделать для новой машины и что проверяется
автоматически. Контракт живёт в `:core:state-machine:api`, харнесс — в
`testSupport/statemachine`, правило — в `docs/verify-module-architecture.py`.

## Что такое машина

Машина — чистая `reduce(state, input)` владельца, у которой есть:

1. `Machine<State, Input, Effect>` из `:core:state-machine:api`: `id`, `space` и мост
   `step`, который за три строки отдаёт то, что уже считает `reduce`. Вложенный `Transition`,
   сам `reduce`, их вызовы и сериализованные формы не трогаются.
2. `StateSpace` — объявление того, что **нельзя вычислить**: позиции (`phases`), входы с
   ветвью Intent/Fact, эффекты, матрица принятия `accepts`, `label(state)`, `name(input)`,
   `name(effect)`, `unknown(state)`, `rejected(effect)`. Целей переходов там нет намеренно:
   они выводятся прогоном `reduce` по образцам, и второго источника правды не возникает.
3. Тест владельца, который строит по образцу на каждую позицию и вход и вызывает
   `verifyStateSpace(machine, states, inputs)`. Образцы лежат в `commonTest` владельца, а не
   в api: фикстуры не должны попадать в поставляемый бинарник, включая web-сборку.

Не машины: `CoordinationRules` и `PlanOrchestrationRules`. Это свёртки документа `Plan` по
журналу без собственного состояния и без эффектов, то есть проекции журнала. Исключение
записано в верификаторе поимённо (`PURE_FOLDS`): новая свёртка должна быть добавлена туда
сознательно.

## Что проверяет харнесс

`verifyStateSpace` прогоняет каждую позицию против каждого входа и держит объявление к
результату:

- **матрица равна редьюсеру**: объявленное принятие совпадает с тем, что `reduce` принял или
  отказал (`rejected(effect)`);
- **замкнутость**: принятый переход обязан привести в объявленную позицию, иначе печатается
  недостающее состояние;
- **`label` инъективна**: представитель позиции опознаётся собственной `label`;
- **достижимость**: каждая неначальная позиция достижима из образца;
- **неизвестный исход**: есть хотя бы одна позиция, где `unknown(state)` истинно;
- **отказ ничего не меняет**: состояние после отказа равно исходному;
- **алфавит полон**: `name(input)` и `name(effect)` — исчерпывающие `when` без `else`, поэтому
  новая ветка не компилируется, пока её не назвали.

### Чего харнесс не видит

`label` — объявленная абстракция, а не бисимуляция. Харнесс видит по одному образцу на
позицию, поэтому ему недоступно:

- **приоритет `label`**, когда состояние держит несколько записей сразу (открытый вопрос и
  запущенный ввод, несколько запусков). Нужен адресный тест: состояние из двух записей и
  явная позиция, которую оно обязано назвать;
- **условия `unknown()` за пределами образцов**: одна запись — образец, а условие может
  срабатывать и в позиции, названной другой, более требовательной записью;
- **отказы по идентичности** (устаревшая ревизия, чужой ключ): принятие не является функцией
  никакой конечной абстракции, и такие случаи остаются в тестах владельца.

Отсутствие этих тестов харнесс не заметит; их нужно писать, и проверять мутациями (ниже).

## Как назвать позиции

- **Позиция называется тем, от чего зависит принятие**, в том порядке, в каком редьюсер
  вычисляет свои условия. Позиция — не полезная нагрузка: два состояния, отличающиеся только
  счётчиком или ревизией, — одна позиция.
- **Разделяй вход по `name(input)`**, если от полезной нагрузки зависит принятие или
  приземление. Иначе позиция, в которую ведёт только один вариант нагрузки, остаётся
  недостижимой из образцов.
- **Позиции с разным долгом не сливаются**: остановка, которой нечего дожидаться, и остановка,
  которой должны ещё эффект, различны, если дальше они принимают разные входы.
- **Матрицу ряда позиций порождай скриптом**, а не набирай руками, если позиций больше
  десятка: харнесс проверит результат по редьюсеру, а опечатка в рукописной строке в
  ProviderTool была найдена именно им.
- **Ветвь Intent/Fact** берётся из типа входа. Если у входа нет разделения (`StageEvent`), ветвь
  объявляет само пространство. Если разделение нужно, а ветви уже сериализованы,
  `OrchestrationEvent` делится маркерными интерфейсами `OrchestrationIntent` и
  `OrchestrationFact` поверх неизменных ветвей.

## Машины без эффекта отказа

`rejected(effect)` должен отличать отказ от принятия. Когда машина отказывает исключением
или собственным результатом, а не эффектом, `step` оборачивает результат: `Effect.Emit(...)`
для принятого и `Effect.Reject(reason)` для отказа. Вызовы прежнего `reduce` не меняются.

- `StageMachine` ловит `IllegalArgumentException` и `NoSuchElementException`,
  `OrchestrationMachine` — `IllegalArgumentException` и `IllegalStateException`: это
  исключения, которыми редьюсер отказывает намеренно.
- `SessionOrganismMachine` оборачивает свой `Output` в `Emit` или `Reject`. Исключение
  `NoSuchElementException` из его редьюсера мостом **не ловится**: тест обходит его
  обёрткой и закрепляет расхождение, чтобы починка редьюсера была заметна.
- `UsageMachine` отображает свой `Transition.rejection` во вложенный `Effect.Reject`.

## Форма данных

Ветви, которые попадают в журнал, кодируются дискриминатором — полным вложенным именем
класса, если нет `@SerialName`. Любой перенос или разделение такого типа без явного имени
портит сохранённые данные. Поэтому имя **закрепляется равным текущему** до любой правки и
проверяется golden-тестом, снятым до аннотации (`OrchestrationEventWireFormatTest`). Короткие
имена вместо длинных не годятся: алиасов дискриминатора нет.

Закреплены поимённо: 28 веток `OrchestrationEvent` и 65 веток планового журнала —
`CoordinationEvent`, `PlanRevisionEvent`, `StageMutation` (ветки лежат в `StageEvent`, поэтому и
имя `...planning.StageEvent.X`) и `FinalAttemptMutation` (`PlanningJournalWireFormatTest`). Тест
называет каждый класс отдельно, потому что набор имён не заметит подмены имён у двух веток.
Закреплены и все ветки `Input` (`Intent` и `Fact`) шести машин, у которых раньше не было ни
одного имени: `NativeLifecycleMachine` 20, `UsageMachine` 7, `CommandCheckMachine` 17,
`SessionOrganismMachine` 40, `PlanningMachine` 54, `TaskWorktreeMachine` 30 — 168 веток, у
каждой машины свой `<Имя>WireFormatTest`. У остальных машин ветки были закреплены владельцами
(`ChatMachine` короткими именами, например `"CreateNotebook"`), и на дереве не осталось ни одной
ветки `Intent` или `Fact` без `@SerialName`.

Закреплены и 4 ветки `PlanningNativeFact` (`PlanningNativeFactWireFormatTest`): он лежит внутри
`PlanningMachine.Fact.NativeObserved` и журналируется вместе с остальным `Input`.

**Не закреплены** значения, чьё имя не оканчивается на `Intent` или `Fact`: `AppRoute`,
`TranscriptBlock`, `CodingImageLocator` и `UsageObservation`. Попадают ли они в хранилище, не
проверялось. Пока имя такой ветки равно полному имени класса, перемещать или переименовывать её
нельзя. Закрепляют так же — имя снимается с кодировщика на нетронутом коде, тест проходит до
аннотации и после неё.

Новую ветку без имени останавливает верификатор: любой `@Serializable` класс или объект, чей
супертип оканчивается на `Intent` или `Fact` (`Fact`, `Machine.Input.Fact`, `OrchestrationIntent`),
обязан нести `@SerialName`. Правило смотрит на объявление, а не на тело `Intent` или `Fact`: ветка
может лежать в другом месте пакета. Оно действует во всех production-модулях, а не в тестах, и
на дереве не имеет исключений. Строку `@SerialName` оно не сверяет: что имя равно прежнему,
доказывает golden-тест, снятый до аннотации, а не верификатор.

## Исправление дефекта машины

Дефект в поведении, за которое отвечает машина, исправляют сверху вниз: **сначала объявление,
потом логика**. `StateSpace` — то, что владелец обещает; `reduce` — то, как он это делает.
Когда они разошлись или обещание неполно, правка редьюсера «по симптому» выбирает поведение
по тому, что случайно делает код, и оставляет дыру в объявлении для следующего дефекта.
Дефект без машины-владельца (отрисовка, платформа, сборка) идёт по `magicpaper-debug` без
этого порядка.

### 1. Найди владельца и разверни его дерево

Владелец — по [CODEMAP](agent-workflows/CODEMAP.md), его `Space` — по каталогу ниже. Дерево —
это позиции, входы, матрица `accepts`, эффекты, `label`, `unknown()` и **соседи**: службы,
которые исполняют эффекты машины и возвращают исход входом `Fact`, и машины, чьи эффекты
приходят к ней Intent'ом. Ни одна машина пока не адресует другую (см. «Чего пока нет»), поэтому
ребро между машинами лежит в службе и находится чтением, а не таблицей.

До чтения `reduce` разверни таблицу: `project(machine, states, inputs)` из
`:core:state-machine:impl`, затем `toMarkdown()` или `toMermaid(showRefusals = true)`. Так
видны позиции, из которых нет выхода, и то, куда `reduce` ведёт каждый вход из образца. Она не
покажет состояние, которого ни один образец не строит: для этого нужен второй образец (шаг 2).

### 2. Назови пробел или противоречие

Правка начинается с имени дефекта из списка ниже. «Редьюсер вернул не то» именем не является.

**Пробел** — в объявлении чего-то нет:

- *нет позиции*: код может стоять в состоянии, которое `label` не называет (`null`), либо две
  позиции с разным долгом слиты в одну;
- *нет входа*: `reduce` или вызывающий отправляет то, чего нет в алфавите;
- *нет решения*: для достижимой пары «позиция × вход» принятие не решалось осознанно, ячейку
  просто снял с кода `reduce`;
- *нет выхода*: из позиции не принимается ни один вход, и она не названа конечной. Харнесс
  проверяет достижимость, но не живость;
- *нет исхода*: у эффекта нет входа `Fact` с его результатом либо нет позиции `unknown()`, и
  после сбоя действие навсегда «в полёте» или молча считается успехом;
- *нет исполнителя*: эффект объявлен, а служба его не исполняет.

**Противоречие** — два источника говорят разное:

- матрица и `reduce` (харнесс ловит сам);
- `label` при нескольких записях: две позиции претендуют на одно состояние, побеждает порядок
  строк;
- `unknown()` срабатывает в позиции, названной другой, более требовательной записью;
- соседи: две машины называют один факт по-разному, например одна считает попытку
  остановленной, а другая идущей;
- объявление и формат на диске: состояние, читаемое из журнала, не попадает ни в одну позицию;
- отказ по идентичности (ревизия, ключ, поколение), который не выражен ни в одной ячейке;
- объявление и документ: `STUDIO-ARCHITECTURE.md`, `STATE-SPACES.md`, видимое пользователю
  поведение.

Если объявление и код расходятся и оба правдоподобны, правым назначает не код, а контракт:
документ, поведение, которое видит пользователь, или ответ владельца задачи. Не решай в пользу
кода потому, что он уже написан.

**Как искать: второй образец.** Зелёный харнесс говорит только о том, что по одному образцу на
позицию объявление совпало с редьюсером. Пробел «нет позиции» и противоречие по идентичности
сидят там, где два состояния носят одно имя, а отвечают по-разному. Найти их можно, построив
второй образец позиции и сравнив его со строкой матрицы:

1. Построй состояние, которое `label` называет той же позицией, но другим путём. Что менять:
   - *путь в позицию*: неизвестный эффект инструмента (`Fact.Quarantine`) и карантин по диагнозу
     дают одну позицию и разное состояние корня;
   - *кто держит запись*: корень или потомок, при том что позиция называет запись, а не держателя;
   - *необязательная часть*: организм с иммунитетом и без него, если владелец допускает и то и
     другое;
   - *идентичность образца*: позиция, в которой образец теста стоит под другим поколением или
     ревизией, чем входы, отвечает по идентичности, а не по позиции. Строй входы этого образца.
2. Прогони по нему все входы и выпиши расхождения: объявлено ли принятие (`position in accepts`)
   и принял ли `reduce`. Отсутствующую запись считай отказом, как считает харнесс.
3. Прежде чем назвать расхождение дефектом, сними артефакт идентичности: входы образца несут
   идентификаторы своего состояния (ключ карантина, ревизию, поколение). Построй вход из самого
   второго образца и повтори. Расхождение, которое исчезло, к позиции не относится.
4. Оставшееся назови по списку: образцы различаются долгом — это *нет позиции*; писатели одного
   факта расходятся — противоречие, и правого назначает контракт.

Такой перебор оставляют тестом, а не разовой пробой: образцы лежат в `commonTest` владельца,
расхождения печатаются одним списком в сообщении утверждения. Пример —
`SessionOrganismSpaceTest.anOrganismThatOwnsAnImmunitySessionAnswersLikeOneThatDoesNot`; второй
образец, который оказался позицией, — `quarantined-unknown` там же. Второй образец, который не
стал позицией, остаётся оговоркой в KDoc `Space` (список «What the declaration cannot express») и адресным тестом.

### 3. Правь объявление и получи красный тест до логики

Измени `<Имя>Space.kt` и образцы в `commonTest` владельца раньше `reduce`. К моменту первой
правки логики обязан существовать красный тест, воспроизводящий дефект:

- пробел или противоречие из числа тех, что видит харнесс, — красный `verifyStateSpace`
  (ячейка, замкнутость, достижимость, `unknown`);
- всё из раздела «Чего харнесс не видит» (приоритет `label`, `unknown()` вне образцов, отказ
  по идентичности) — адресный тест: состояние из нескольких записей и явная позиция, которую
  оно обязано назвать.

Если после правки объявления всё зелёное, значит дефект не в объявлении или объявление его не
выражает. Вернись к шагу 2, а не переходи к логике вслепую.

Не приводи объявление в соответствие с редьюсером, чтобы харнесс позеленел: это ослабление
проверки под другим именем. Так поступают, только если шаг 2 установил, что `reduce` прав, а
объявление ошибочно, и правка так и названа.

Новая ветка `Intent` или `Fact` несёт `@SerialName`; существующие имена не меняются (раздел
«Форма данных»). Новая позиция должна называть и состояния, уже сохранённые в журнале.

### 4. Потом логика, снизу вверх

1. Чистый `reduce`, пока красный тест не позеленеет.
2. Служба владельца: исполнение эффекта, возврат исхода `Fact`, отмена, восстановление.
3. Интерфейс.

Не добавляй в службу `if`, который прикрывает пробел машины: охрана в адаптере прячет дыру от
харнесса, и дефект вернётся с другим входом.

### 5. Закрепи и доложи

- Тест шага 3 остаётся. Исправленную ячейку проверь мутацией (следующий раздел).
- Запусти проверки владельца, затем `python3 docs/verify-module-architecture.py --self-test`;
  при изменении публичного контракта расширь набор по [VERIFICATION](agent-workflows/VERIFICATION.md)
  и сверь новый отказ с разделом «Долги» в [STUDIO-ARCHITECTURE](STUDIO-ARCHITECTURE.md).
- В отчёте назови: пробел или противоречие и его вид, что изменено в объявлении, что в
  логике, красный и зелёный результат теста, что осталось непроверенным.

## Проверка мутациями

Зелёный харнесс говорит только о том, что объявление совпало с редьюсером. Чтобы убедиться,
что тесты сами не пусты, объявление ломают: переворачивают ячейку матрицы, схлопывают или
переворачивают приоритеты `label`, убирают условия `unknown()`. Каждый выживший мутант
получает адресный тест, после чего мутация повторяется. Скрипта мутаций в репозитории нет:
перед правкой файл копируют рядом, ломают вручную, а затем возвращают копию и проверяют `cmp`.
`git checkout` для этого не годится: он вернёт файл к коммиту и сотрёт саму правку дефекта.

## Журнал

`MachineJournal<S, I, E>` в `:core:storage:api` — общий журнальный контракт: `replay`, `append`
с исходами Accepted, Refused и Conflict, `JournalOutcomeUnknown` и `InputCodec`. Потерянное
подтверждение согласуется чтением обратно. На него переведён один раннер —
`DefaultRequestPinService`; остальные девять **не переведены**.

План останавливает перевод, если после первого раннера общего кода меньше 150 строк при более
чем пяти хуках. Измерено: адаптер — 66 строк кода (122 с KDoc); перевод `DefaultRequestPinService`,
самого близкого по устройству раннера, дал `-30 +17`, то есть минус 13 строк из 292, и ему
понадобились только `codec`, `elideNoOps` и `operation`. Остальным по чтению кода нужно ещё:
проверка, что ревизия принадлежит ожидаемому потоку (`BrowserInputJournal`,
`MediaOperationJournal`), инвариант состояния после каждой записи (`state.owner == owner`,
`operation.id == id`), уникальность id записей на протяжении одного проигрывания, сверка
префикса журнала при чтении обратно (`BrowserInputJournal`) и особая политика отмены (оба
не пишут внутри `NonCancellable`, а после подтверждённой записи пробрасывают отмену). Это
больше пяти хуков при общем коде меньше 150 строк, то есть условие остановки выполнено.
Решено остановиться на одном раннере: остальные девять остаются со своим кодом, а адаптер
используется одним владельцем. Возвращаться к переводу имеет смысл, только если у нескольких
раннеров одновременно появится общая причина менять их журнальный код; тогда сначала стоит
решить, какие из перечисленных хуков войдут в адаптер, а не расширять его по одному.

## Чего пока нет

- **`MachineKey` и `Tell`** (адресация соседней машины). Вводятся вместе с первым настоящим
  ребром между машинами; сегодня ни одна машина другую не адресует.
- **Общей сгенерированной страницы**. `:core:state-machine:impl` рендерит одну машину:
  `project(machine, states, inputs)` разрешает объявление в таблицу, `toMermaid()` и
  `toMarkdown()` дают диаграмму и матрицу. Свести все машины в один файл нельзя без
  образцов, а они лежат в тестах владельцев; страница строится из теста, не из api.

## Как добавить машину

1. Реализуй `Machine<State, Input, Effect>`: `id` — литерал (имя не выводится из класса: Wasm
   опускает объемлющий тип), `space`, мост `step`.
2. Объяви пространство в `<Имя>Space.kt` рядом с машиной, в `commonMain` api.
3. Напиши `<Имя>SpaceTest.kt` в `commonTest` с образцами и `verifyStateSpace`; добавь
   адресные тесты на приоритет `label` и `unknown()`.
4. Подключи в `build.gradle.kts` api-модуля `api(project(":core:state-machine:api"))` и
   `commonTest { kotlin.srcDir(rootProject.file("testSupport/statemachine")) }`.
5. Добавь строку в каталог ниже.

Правило `docs/verify-module-architecture.py` требует ровно этого для каждого файла с
`fun reduce(` в api-модуле или в `:core:model`, кроме `PURE_FOLDS`:

- файл объявляет `object|class … : Machine<…>`;
- в тестовых source set модуля есть файл, где вызван `verifyStateSpace(` и названа эта
  машина (обёртка над машиной, названная в том же файле, подходит);
- машина есть в таблице каталога строкой ``| `Имя` | … |``.

Проверка текстовая: она не доказывает, что тест запускается и проходит, — это делает
Gradle. Она закрывает другое: новая машина не появится без объявления, харнесса и записи здесь.

## Каталог

| Машина | Модуль | Пространство |
| --- | --- | --- |
| `DraftMachine` | `:core:storage:api` | `DraftSpace` |
| `UsageMachine` | `:core:ai:api` | `UsageSpace` |
| `OrchestrationMachine` | `:core:model` | `OrchestrationSpace` |
| `PlanningMachine` | `:magic-agent:planning:api` | `PlanningSpace` |
| `StageMachine` | `:magic-agent:planning:api` | `StageSpace` |
| `CodingMachine` | `:magic-agent:runtime:api` | `CodingSpace` |
| `BrowserMachine` | `:magic-agent:browser:api` | `BrowserSpace` |
| `CommandCheckMachine` | `:magic-agent:checks:api` | `CommandCheckSpace` |
| `ComputerMachine` | `:magic-agent:computer:api` | `ComputerSpace` |
| `SessionOrganismMachine` | `:magic-agent:organism:api` | `SessionOrganismSpace` |
| `TaskWorktreeMachine` | `:magic-agent:workspace:api` | `TaskWorktreeSpace` |
| `NativeLifecycleMachine` | `:backend-agents:api` | `NativeLifecycleSpace` |
| `SettingsMachine` | `:feature:settings:api` | `SettingsSpace` |
| `PluginMachine` | `:feature:plugins:api` | `PluginSpace` |
| `SkillMachine` | `:feature:skills:api` | `SkillSpace` |
| `MediaGenerationMachine` | `:magic-common:media:api` | `MediaGenerationSpace` |
| `NavigationMachine` | `:magic-common:navigation:api` | `NavigationSpace` |
| `QuestionnaireMachine` | `:magic-common:questionnaire:api` | `QuestionnaireSpace` |
| `RequestPinMachine` | `:magic-common:request-pins:api` | `RequestPinSpace` |
| `ProviderToolMachine` | `:magic-common:tools:api` | `ProviderToolSpace` |
| `ChatMachine` | `:magic-chat:api` | `ChatSpace` |
