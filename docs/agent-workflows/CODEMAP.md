# Карта входа для разработки

Выберите строку по задаче. Начните с указанного контракта/реализации и теста.
Поддереьевой AGENTS.md читайто только для уникальных ограничений модуля;
общие правила — в корневом AGENTS.md. Полный граф — [MODULES.md](../MODULES.md);
команды — [VERIFICATION.md](VERIFICATION.md). Не загружайте соседние области,
пока изменение или найденный вызов их не затрагивает.

**Минимизация вызовов:** для простой задачи начните сразу с кода по колонке
«Начать с»; не читайте MODULES.md, VERIFICATION.md и skills, если задача
однозначна. Читайте VERIFICATION.md только для подбора команд проверки.

В таблице `commonMain`, `jvmMain` и тестовые source set содержат пакет
`kotlin/io/aequicor/magicpaper/`. Сохранённое имя пакета не определяет Gradle-владельца:
после миграции одинаковые пакеты могут находиться в разных модулях.
Корень приложения — `:app` в `app/`; прежнего Gradle project `:shared` и alias нет.

| Задача | Владелец / каталог | Начать с | Ближайшая проверка |
| --- | --- | --- | --- |
| Маршрут, Back/Forward, ссылка, диалог | [app/navigation](../../app/src/commonMain/kotlin/io/aequicor/magicpaper/navigation) | `AppRoute.kt`, `NavigationJournal.kt`, `RootComponent.kt`; browser: `BrowserHistoryBridge.kt` | `:app:jvmTest --tests '*RootComponentTest' --tests '*RootDialogLifecycleTest'` |
| Прокрутка и сохранение состояния экрана | [app/navigation](../../app/src/commonMain/kotlin/io/aequicor/magicpaper/navigation), [chat](../../magic-chat) | `VisitPresentation.kt`; `magic-common/transcript/.../ui/components/MessageHistoryActions.kt`; lazy Markdown в `designSystem` | `:app:jvmTest --tests '*VisitPresentation*Test'`; runtime `jvmTest --tests '*MessageHistoryActionsRenderTest'`. Проверять с visit registry, а не только изолированной лентой |
| Koin, startup, reset, shutdown | [app/di](../../app/src/commonMain/kotlin/io/aequicor/magicpaper/di) | `AppRuntime.kt`, `Dependencies.kt`, затем платформенный `Dependencies.*.kt` | `:app:jvmTest --tests '*RuntimeLifecycleTest'` |
| Оболочка, sidebar, выбор фичи | [app](../../app/src/commonMain/kotlin/io/aequicor/magicpaper) | `App.kt`, `navigation/AppRoot.kt`, `ui/AppContributions.kt`, `ui/screens/UnifiedSidebar.kt`; JVM `ui/NativeAppContributions.kt`, `ui/screens/NativeSidebarProjection.kt` | `:app:jvmTest --tests '*AppContributionsTest' --tests '*UnavailableAppRouteRenderTest' --tests '*UnifiedSessionFeedRenderTest' --tests '*SidebarRestorationTest' --tests '*AppShellRenderTest'` |
| Чат, отправка, composer | [chat](../../magic-chat) | `api/.../ui/ChatComponent.kt`, `ChatService.kt`; `impl/.../ui/DefaultChatService.kt`, `DefaultChatComponent.kt`, `ChatQuestionnaires.kt`; shared `transcript/.../PlanningQuestionsDock.kt` | `:magic-chat:impl:jvmTest`; host integration: `:app:jvmTest --tests '*ChatServiceLifecycleTest' --tests '*ChatInputQueueTest' --tests '*ChatResearchTest'` |
| Вопросы и источники исследования | [chat](../../magic-chat), [контракт](../desktop-ui/RESEARCH-CHAT.md) | `api/.../ui/ChatState.kt`, `ChatService.kt`; `impl/.../ui/screens/ResearchWorkspace.kt`, `ResearchSourcesPane.kt`, `magic-common/research/.../domain/ChatResearch.kt`; модели `ChatSession`, `ResearchResource` | `:app:jvmTest --tests '*ChatResearchTest'`; owner `jvmTest --tests '*ResearchWorkspaceRenderTest' --tests '*ResearchSourcesPaneRenderTest' --tests '*ChatResearchContextTest' --tests '*ResearchPageReaderTest'` |
| Кодинг, сессия, история, checkpoint | [runtime API](../../magic-agent/runtime/api), [runtime impl](../../magic-agent/runtime/impl) | `domain/CodingMachine.kt`, `CodingProjectCommands.kt`, readonly `CodingProjectRepository.kt`; журнал: `data/coding/CodingJournalStore.kt`, `StoredCodingPayloads.kt`; снимки совместимости: `JsonCodingProjectRepository.kt`; consumer: `ui/DefaultCodingService.kt`; сборка: `di/CodingFeatureFactory.kt` | API `jvmTest --tests '*CodingMachineTest'`; impl `*CodingJournalStoreTest`, `*ConversationHistoryTextTest`; app `*CodingResumeTest`, `*ConversationHistoryTest`, `*SessionInputQueueTest`, `*CodingWorktreeTest` |
| Реактивная проекция планирования | [coding](../../magic-agent/runtime/impl) | `ui/PlanningSessionProjection.kt`, `StageChatState.kt`; снимок: `magic-agent/runtime/api/.../ui/CodingState.kt` (`CodingUi.planning`); публикация: `DefaultCodingService.observeRuntime` | `:magic-agent:runtime:impl:jvmTest --tests '*PlanningSessionProjectionTest' --tests '*PlanningSessionStatusTest' --tests '*StageChatStateTest'`; renders: `*PlanningProposalRenderTest`, `*OrchestrationFailureRenderTest` |
| Панель планирования: фоновые запросы и lifecycle | [coding](../../magic-agent/runtime/impl) | `plugins/builtin/PlanningPanelController.kt`, `DecisionPlanningPlugin.kt`; application scope, pure `PlanningPanelState.planFor` | `:magic-agent:runtime:impl:jvmTest --tests '*PlanningPanelControllerTest' --tests '*PlanningWizardRenderTest'`; граница экрана: `:app:jvmTest --tests '*CodingComponentNavigationTest' --tests '*QuestionnaireViewModelTest'` |
| Worktree задачи, пул, автослияние | [session worktrees](../../feature/session/WORKTREES.md) | машина/контракты: `magic-agent/workspace/api/.../domain/TaskWorktreeMachine.kt`, `TaskWorkspace.kt`; journal/owner: `magic-agent/workspace/impl`; parent coordinator: `magic-agent/runtime/impl/.../domain/TaskWorktreeService.kt`; JVM `data/planning/GitTaskWorkspace.kt` | `:magic-agent:workspace:api:jvmTest :magic-agent:workspace:impl:jvmTest`; `:magic-agent:runtime:impl:jvmTest --tests '*GitTaskWorkspaceTest'`; `:app:jvmTest --tests '*CodingWorktreeTest'` |
| Команды проверки, защита исходников, процессные receipts | [checks](../../magic-agent/checks) | API `domain/checks/CommandCheckMachine.kt`, `CommandChecks.kt`; impl `data/checks/DefaultCommandChecks.kt`, `CheckInputJournal.kt`; policy/native `data/research/` | `:magic-agent:checks:api:jvmTest :magic-agent:checks:impl:jvmTest`; runtime `*TaskWorktreeIntegrationChecksTest`, `*ResearchCheckBridgeTest`; OS sandbox отдельно opt-in |
| Удержание рабочей папки и точное освобождение | [workspace](../../magic-agent/workspace), [runtime](../../magic-agent/runtime/impl) | API `domain/PlanningWorkspace.kt` (`WorkspaceLease`); impl JVM `domain/LocalPlanningWorkspace.kt`; runtime JVM `data/planning/GitPlanningWorkspace.kt`; consumers `SessionCodingWorkspace`, `SessionTreeRuntime`, `SessionIntegrationWorkspaces`, `TaskWorktreeService` | Workspace impl `*LocalPlanningWorkspaceTest`; runtime `*GitPlanningWorkspaceTest`, `*PlanningWorkspaceOwnershipTest`, `*SessionCodingWorkspaceTest`, `*SessionIntegrationTest`; app `*CodingWorktreeTest` |
| Планирование, дерево, вмешательство | [coding/domain](../../magic-agent/runtime/impl/src/commonMain/kotlin/io/aequicor/magicpaper/domain) | `OrchestrationService.kt`, `SessionTreeRuntime.kt`; далее конкретный store/workspace по вызову | Owner `jvmTest --tests '*PlanningChatServiceTest'` либо соответствующий `*Session*Test` |
| Предложение/подтверждение плана, восстановление назначений, legacy-связи | [planning API](../../magic-agent/planning/api), [runtime](../../magic-agent/runtime/impl) | `magic-agent/planning/api/.../domain/planning/PlanOrchestrationRules.kt`; адаптеры `domain/OrchestrationService.kt`, `PlanningExecutionService.kt` | Planning API `jvmTest --tests '*PlanOrchestrationMachineTest'`; coding `jvmTest --tests '*PlanningChatServiceTest' --tests '*PlanningExecutionServiceTest' --tests '*PlanningRetryCheckpointTest' --tests '*OrchestrationToolsTest'` |
| Очередь оркестратора, вопросы, паузы, квитанции координации | [model](../../core/model), [coding](../../magic-agent/runtime/impl) | `core/model/.../domain/planning/OrchestrationMachine.kt`, `magic-agent/planning/api/.../domain/planning/CoordinationRules.kt`; адаптер `domain/OrchestrationService.kt` | Model `jvmTest --tests '*OrchestrationMachineTest'`; planning API `*CoordinationMachineTest`; coding `jvmTest --tests '*OrchestrationInputAcceptanceTest' --tests '*PlanningChatServiceTest' --tests '*OrchestrationToolsTest'` |
| Переходы этапа, проверка, повтор, объединение, отмена | [planning API](../../magic-agent/planning/api), [runtime](../../magic-agent/runtime/impl) | `magic-agent/planning/api/.../domain/PlanningMachine.kt`, `domain/planning/StageRules.kt`, `FinalAttemptRules.kt`; `core/model/.../StagePrompt.kt`; адаптер `domain/PlanningExecutionService.kt` | Planning API `jvmTest --tests '*StageMachineTest' --tests '*PlanningStageAuthorityTest' --tests '*FinalAttemptRulesTest'`; coding `jvmTest --tests '*PlanningExecutionServiceTest' --tests '*PlanningCheckpointReloadTest'` |
| Классификация повторных сбоев и разрешённые стратегии | [model](../../core/model), [coding](../../magic-agent/runtime/impl) | `domain/planning/PlanStrategy.kt`; `domain/PlanStrategyClassifier.kt`, `data/planning/PlanStrategyJournal.kt`; подключение `CodingRuntimeGraph`, `PlanningExecutionService` | Model `*PlanStrategyTest`; coding `*PlanStrategyClassifierTest`, `*PlanJournalReaderTest`, `*PlanningExecutionServiceTest` |
| Plan как проекция журнала, редакции, checkpoint и удаление | [planning API](../../magic-agent/planning/api), [planning impl](../../magic-agent/planning/impl), [storage](../../core/storage) | `domain/planning/PlanProjection.kt`; `magic-agent/planning/api/.../PlanningMachine.kt`, `data/planning/PlanJournalProjection.kt`; planning impl `data/planning/DefaultPlanningStore.kt`; storage `EventJournal`, `DurableEventJournal` | Model `*PlanProjectionTest`; coding `*PlanningJournalProjectionTest`, `*PlanningStoreCacheTest`, `*PlanningEventJournalTest`; storage `*JournalRevisionTest`, `*DurableEventJournalTest`; JS/Wasm `IndexedDbPersistenceTest` |
| Журнал намерений, неизвестный исход, восстановление | [coding](../../magic-agent/runtime/impl) | planning API `data/planning/PlanningStore.kt`, `PlanJournalReader.kt`; planning impl `DefaultPlanningStore.kt`; `domain/PlanningJournalRecovery.kt`, `PlanningExecutionService.kt`, `PlanRetryNativeRecovery.kt`; модель `StageResumption` | Owner `jvmTest --tests '*PlanJournalReaderTest' --tests '*PlanningJournalRecoveryTest' --tests '*PlanningExecutionServiceTest' --tests '*NativeReceiptRecoveryTest' --tests '*SessionQuarantineRecoveryTest'` |
| Общая презентация сессий | [transcript](../../magic-common/transcript) | `ui/screens/SessionTranscript.kt` (`MessagesList`, `Composer`), `ui/components/` (вложения, закрепления, действия над историей), `domain/UnreadTracker.kt`, `ConversationHistory.kt`; нативные заголовки/архивация — `magic-agent/runtime/impl` | `:magic-chat:impl:jvmTest` и `:magic-agent:runtime:impl:jvmTest` — модуль пока покрыт тестами потребителей |
| Исследование: маршрутизация, источники, общий промпт | [research](../../magic-common/research) | `domain/ChatResearch.kt`, `ResearchRequest.kt`, `ResearchFollowUps.kt`, `ResearchSourceAccess.kt`; `data/ResearchPageReader.kt` | `:magic-common:research:jvmTest`; HTTP-chat integration: `:magic-chat:impl:jvmTest --tests '*ResearchPageReaderTest'` |
| Сборка системных инструкций, навыков и контекста | [prompts](../../magic-common/prompts), [native adapter](../../magic-agent/runtime/impl) | Общие `ChatPrompts`, `ReadOnlyPrompts`, `ToolPrompts`; desktop `data/coding/CodingSystemPrompts.kt` добавляет инструкции backend, worktree, browser и computer | `:magic-common:prompts:jvmTest`; runtime `*CodingSystemPromptGoldenTest`, `*CodingSystemPromptsTest`, `*SessionContextRuntimeTest`; chat `*GatewaySessionRuntimeTest` |
| Опросники: ответы, журнал, восстановление | [questionnaire](../../magic-common/questionnaire) | API `QuestionnaireMachine`, `RuntimeQuestionnaireService`; impl `DefaultRuntimeQuestionnaireService`; legacy imports и секреты только в текущем run | `:magic-common:questionnaire:api:jvmTest :magic-common:questionnaire:impl:jvmTest`; runtime `*Questionnaire*Test` |
| Закреплённые запросы: анализ и хранение | [request-pins](../../magic-common/request-pins) | API `RequestPinMachine`, `RequestPinService`; impl `DefaultRequestPinService`, `JsonRequestPinRepository`; journal Intent/Fact, unknown result, delete/reset | `:magic-common:request-pins:api:jvmTest :magic-common:request-pins:impl:jvmTest`; app `*RequestPinViewModelTest` |
| Инструменты: словарь, схемы, доступ | [tools](../../magic-common/tools) | `ToolCatalog`, `SessionToolCatalog`, `ToolDefinitions` (матрица доступа), `QuestionnaireContract`, `ToolRejections`, `CustomOrchestration`, `DefaultCustomOrchestration` | `:magic-common:tools:impl:jvmTest` |
| Исполнение инструментов и эффекты | [tools](../../magic-common/tools), [coding](../../magic-agent/runtime/impl) | Общие `ToolExecutor`, `ToolSession`, `ToolReceiptIntegrity`, `MediaToolReceiptOwner`; нативные `ToolSessionFactory`, `ApplicationToolCommands`, `ToolEnabledCodingRuntime`, `AgentToolBridge`; planning эффекты у `OrchestrationService` | `:magic-common:tools:impl:jvmTest`; runtime `*SessionAuthorityToolTest`, `*Tool*Test`, `*MediaToolRecoveryIntegrationTest` |
| История, очередь, остановка и восстановление обычного чата | [chat](../../magic-chat) | API `ChatMachine`, `ChatHistoryCommands`, `ChatPayloadStore`; impl `ChatJournalStore`, `StoredChatPayloads`, `DefaultChatService`; `ChatRepository` только читает, `JsonChatRepository` хранит совместимый кэш | chat API `*ChatMachineTest`; impl `*ChatJournalStoreTest`, `*ChatSavedResponseIntegrationTest`; app `*ChatInputQueueTest`, `*ChatServiceLifecycleTest`, `*ChatResearchTest`, `*ConversationHistoryTest` |
| HTTP-чат с инструментами, provider continuation | [tools](../../magic-common/tools), [chat](../../magic-chat), [AI](../../core/ai) | `ProviderToolMachine`, `DefaultProviderToolLoop`, `StoredProviderToolOutputs`, `DefaultChatToolSessions`, `GatewaySessionRuntime`, `LlmGateway.turn`, `LlmToolWire` | tools API/impl `jvmTest`; chat `*ProviderChatToolsTest`; AI `*LlmToolTurnTest`; app `*ChatInputQueueTest`, `*ChatResearchTest` |
| Браузер агента, Google, HTML-проверки | [browser API/impl](../../magic-agent/browser) | `BrowserMachine`, `BrowserSessions`; `BrowserInputJournal`, `BrowserToolSession`, `HtmlValidation`; runtime `data/tools/AgentToolBridge`; [владение и проверки](../AGENT-BROWSER.md) | `:magic-agent:browser:api:jvmTest :magic-agent:browser:impl:jvmTest`; runtime `*BrowserToolBridgeTest`, `*AgentToolBridgeTest`; настоящий Chromium: browser impl `-Pmagicpaper.browser.native=true --tests '*BrowserNativeTest'` |
| Ручное чтение недоступного источника | [chat](../../magic-chat) | API `ResearchPageBrowser`; `ui/ResearchBrowserRecovery`, `ui/screens/ResearchBrowserDialog`; JVM [browser impl](../../magic-agent/browser/impl) `data/browser/DesktopResearchPageBrowser` | `:magic-chat:impl:jvmTest --tests '*ResearchBrowserRecoveryTest'`; Chromium: `:magic-agent:browser:impl:jvmTest -Pmagicpaper.browser.native=true --tests '*ResearchPageBrowserNativeTest'`; `:app:jvmTest --tests '*ChatResearchTest'` |
| Pi/Codex, subprocess, wire protocol | [backend-agents](../../backend-agents), [runtime adapter](../../magic-agent/runtime/impl/src/jvmMain) | backend API/factory, lifecycle `JournaledBackendAgent`; Pi/Codex implementations; runtime `GenericNativeRuntime`, `DesktopCodingRuntime`, `OrderedCodingFlow`; [ENGINES.md](../ENGINES.md) | `:backend-agents:pi:nodeProtocolTest`; lifecycle/Pi/Codex `jvmTest`; runtime `*NativeRuntimeDispatchTest`, `*GenericNativeRuntimeTest`, `*OrderedCodingFlowTest`, `*CodingWakeGuardTest` |
| Computer/application-use, доступ и native UI | [computer API/impl](../../magic-agent/computer) | API `domain/ComputerMachine.kt`, `ComputerUse.kt`; `ComputerAuthority`, `DesktopComputerUse`, `DesktopCaptureRequest`, `ApplicationUse`, `NativeApplicationDesktop`; [протокол и проверки](../COMPUTER-USE.md) | `:magic-agent:computer:api:jvmTest :magic-agent:computer:impl:jvmTest`; policy/restore: `:app:jvmTest --tests '*AutomationPolicyTest' --tests '*SessionInputQueueTest' --tests '*ChatInputQueueTest'` |
| Native-настройки движков и компьютера | [runtime](../../magic-agent/runtime/impl) | `NativeSettingsComponent`, `EnginesSettings`, `ComputerSettings`, `ComputerPermissionController`; assembly `nativeSettingsContributions`; API `ComputerPermissions`; JVM `DesktopComputerPermissions` | Runtime `*ComputerSettingsRenderTest`, `*ComputerPermissionControllerTest`, `*AutomationSettingsRenderTest`; computer impl `*DesktopComputerPermissionsTest`; app `*EnginesSettingsRenderTest`, `*SettingsContributionsTest`, `*AppRouteCodecTest` |
| Настройки, формы, импорт/экспорт | [settings](../../feature/settings) | API `SettingsMachine`, `SettingsCommands`, read-only `SettingsRepository`/`LlmProfileRepository`/`ModelDossierRepository`; единственный writer `DefaultSettingsConfiguration` и private `SettingsInputJournal`; UI `DefaultSettingsService`, `SettingsDrafts`, `SettingsContributions`; native runtime участвует через `SettingsRuntimeParticipant` | Settings API/impl `jvmTest` (`*SettingsMachineTest`, `*SettingsConfigurationOwnerTest`, `*SettingsInputJournalTest`, `*SettingsProviderOperationsTest`); app `*NativeSettingsApplicationTest`, `*AutomationPolicyTest`, `*SettingsContributionsTest` |
| Секреты, profile migration | [settings/storage](../../feature/settings/impl/src/commonMain/kotlin/io/aequicor/magicpaper/data/storage) | `SettingsCredentialRecords` разделяет opaque references в journal и SecretStore; `JsonSettingsRepository`, `JsonLlmProfileRepository`, `JsonModelDossierRepository` только legacy import/cache; пределы моделей: `core/model` `ModelLimitCatalog.kt` | Settings owner `*SettingsConfigurationOwnerTest`, storage credential tests; legacy migration только при virgin journal, reopen/failure/cancellation/cleanup |
| Черновики, вложения, постоянная запись | [storage](../../core/storage) и [platform](../../core/platform) | `api/.../data/storage/Persistence.kt`, `DraftSession.kt`; `impl/.../data/storage/DurablePersistence.kt`, `DurableResetBarrier.kt`; platform composer helper | `:core:storage:impl:jvmTest`, `:core:platform:jvmTest`; storage impl `jsBrowserTest`/`wasmJsBrowserTest` отдельно: IndexedDB reset/миграции, нужен Chromium |
| LLM, поиск, usage | [ai](../../core/ai) | `LlmGateway.turn` и `core:model/LlmToolTurn` DTO; HTTP tool dialects `LlmToolWire.kt`; `UsageMachine`/`UsageObservation` в AI API, журнал `UsageInputJournal` и ledger в `impl`; каталог моделей: `ModelDirectories.kt`, `DeclaredLimitsModelDirectory.kt` | `:core:ai:api:jvmTest :core:ai:impl:jvmTest` (`UsageMachineTest`/`UsageJournalTest`: ACK, reset, replay; MockEngine `LlmToolTurnTest`: calls/results, signatures, usage) |
| Изображения/видео в сессии, media provider, placeholder, playback | [media](../../magic-common/media), [media guide](../MEDIA-GENERATION.md): ai, storage, settings, Paper | `MediaGenerationService`, `DefaultMediaGenerationService`, `HttpMediaGenerationGateway`; модель `GeneratedMedia` / `TranscriptContent`, `GeneratedMediaView`, `PaperGeneratedMedia` | Common media `jvmTest`; runtime `*MediaToolRecoveryIntegrationTest`, `*GeneratedMediaTranscriptRenderTest`, `*SessionMediaToolOptionsRenderTest`; DS `*PaperGeneratedMediaTest`, `*PaperMediaImageDecodingTest`; native opt-in `-Pmagicpaper.media.native=true :designSystem:jvmTest --tests '*PaperVideoNativeTest'` |
| Документация приложения | [docs feature](../../feature/docs) | `DocsComponent.kt`, `DefaultDocsComponent.kt`, repository по API | `:feature:docs:impl:jvmTest` |
| Плагины и их включение | [plugins](../../feature/plugins) | API `plugins/PluginMachine.kt`, `PluginPreferences`, `PluginRegistry.kt`; impl `plugins/PluginInputJournal.kt`, `ui/DefaultPluginService.kt`; импорт/экспорт через owner commands | `:feature:plugins:api:jvmTest :feature:plugins:impl:jvmTest`; settings import/reset: `:feature:settings:impl:jvmTest` |
| Навыки внутри приложения | [skills feature](../../feature/skills) | API `SkillMachine`/`SkillCommands`, read-only `SkillRepository`; common catalog writer `SkillStore` + private `SkillInputJournal`; `SkillInstructionRuntime.kt`, `ProjectSkills.kt`; `DefaultSkillsComponent.kt`, нужный panel/adapter | `:feature:skills:api:jvmTest :feature:skills:impl:jvmTest` (`SkillMachineTest`, `SkillJournalIntegrityTest`, `SkillCatalogOwnerTest`, `CommonSkillsOwnerUiTest`); package/experience пока отдельный незавершённый срез |
| Визуальная верстка и каталог Paper | [paper-plugin](../../tools/paper-plugin), [paper-editor](../../tools/paper-editor) | `PaperDesignPlugin`, `PaperFixtures`; [инструкция](../../tools/paper-editor/README.md) | `./gradlew -PpaperEditor=true :tools:paper-plugin:test :tools:paper-editor:compileKotlin` |
| Макет по запросу обычного чата | [runtime](../../magic-agent/runtime), [chat](../../magic-chat), [paper-editor](../../tools/paper-editor) | Native `LayoutEditor`, `LayoutChatAgent`, `LayoutChatExtension`, `DesktopLayoutEditor`; common `ChatResponseExtension`, `DefaultChatService`; launcher `Main`, `PaperAgentCommands` | `:magic-agent:runtime:impl:jvmTest --tests '*Layout*Test'`; `:app:jvmTest --tests '*ChatLayoutWorkflowTest'`; [native-проверка](VERIFICATION.md) |
| Полосы прокрутки: списки, формы, Markdown, редакторы | [designSystem](../../designSystem), [контракт](../desktop-ui/SCROLLBARS.md) | `PaperScroll.kt`, `skikoMain/.../PaperScrollbar.skiko.kt`, `jvmMain/.../PaperBasicTextField.jvm.kt` | `:designSystem:jvmTest --tests "*PaperScrollbarTest" --tests "*PaperMarkdown*Test"`; consumer `ResearchScrollRegressionTest`, `ResearchWorkspaceRenderTest` |
| Потоковый ответ в чате и журнале прогона: плавность, слежение за концом | [designSystem](../../designSystem), [контракт](../desktop-ui/STREAMING-CHAT.md) | `ChatScroll.kt`, `StreamingText.kt`, `MarkdownDocument.kt`, `ChatMarkdown.kt`, `InlineMessage.kt`; consumer `CodingScreen.kt`, `ChatScreen.kt` | `:designSystem:jvmTest --tests "*StreamingText*"`; consumer `CodingChatScrollTest`, `ChatScrollToBottomTest`, `LargeCodingChatRenderTest` |
| Контрол, тема, Markdown, окно | [designSystem](../../designSystem) | Paper API + конкретный consumer; [brandbook](../desktop-ui/BRANDBOOK.md) | Paper verifier + `:designSystem:jvmTest --tests '*ИМЯ_ТЕСТА*'` + consumer render |
| Общая модель / сериализация | [model](../../core/model) | Тип и его serializer; callers только для изменённого контракта | `:core:model:jvmTest`, тесты владельцев чтения/записи |
| Gradle, зависимости | [build-logic](../../build-logic) | conventions; `settings.gradle.kts`, `gradle/libs.versions.toml`, build-файл владельца | Architecture verifier, затем нужные платформы |
| Native links / упаковка | [desktopApp](../../desktopApp), [androidApp](../../androidApp), [webApp](../../webApp) | Desktop `main.kt`/`DesktopActivationBroker.kt`/`packaging`; Android `MainActivity.kt`/manifest; Web `main.kt`/webpack | Платформенные строки в verification map |

`...` в строках фич означает `src/commonMain/kotlin/io/aequicor/magicpaper`.
Перед Gradle-аргументами из таблицы добавляйте `./gradlew` из корня репозитория.
Заменяйте явные placeholders реальным классом теста.

## Поиск без повторного обхода проекта

Пример для чата:

```sh
rg --files magic-chat -g '*.kt' -g '!**/build/**'
rg -n 'sendMessage|composerDraft' magic-chat/impl/src magic-chat/api/src
```

Если меняется API, ищите его имя в конкретных соседних API/impl и root wiring.
Проверяйте контекст найденного вызова; совпадение строки само по себе не доказывает
владение. Не ищите содержимое секретных хранилищ для диагностики обычного кода.

## Поддержка этой карты

При переносе публичной точки входа обновите её строку и соответствующий AGENTS.md.
Не храните здесь текущие статусы тестов, полные логи или копии исходников.
Проектные development-навыки в `skills/` не являются каталогом навыков пользователя
внутри MagicPaper и не должны автоматически устанавливаться в него.
